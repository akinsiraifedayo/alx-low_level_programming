#include "main.h"
#include <stdio.h>
#include <string.h>
/**
* _strlen - change value of a variable pointer is pointing to to 98
*
* @s: takes in the char input
*/
int _strlen(char *s)
{
	strlen(s);
}
