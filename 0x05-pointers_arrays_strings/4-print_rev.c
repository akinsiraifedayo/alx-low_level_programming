#include "main.h"
#include <stdio.h>
#include <string.h>
/**
* _puts- prints a string
*
* @str: takes in the string
* Return: 0
*/
void print_rev(char *s)
{
	int i = 0;

	for (i = 0; s[i] != 0; i++)
	{

	}
	while (i != 0)
	{
		printf("%c", s[i]);
		i--;
	}
	printf("\n");
}
