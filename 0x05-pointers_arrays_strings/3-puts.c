#include "main.h"
#include <stdio.h>
/**
* _puts - change value of a variable pointer is pointing to to 98
*
* @str: takes in the string input
* Return: 0-
*/
int _puts(char *str)
{
	char me[] = "me";

	me = str[];
	printf("%s\n", me);
	return (0);
}
